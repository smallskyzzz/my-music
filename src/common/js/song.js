export default class Song {
  constructor({name, artist, id, duration, image}) {
    this.name = name
    this.artist = artist
    this.id = id
    this.duration = duration
    this.image = image
  }
}
